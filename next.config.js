/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    // distDir: '../../efs-studio',
    // basePath: '/efs-studio',
    // assetPrefix: '/efs-studio/',
}

module.exports = nextConfig
